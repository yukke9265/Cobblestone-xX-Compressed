# Copilot Instructions

プロジェクト共通の方針はリポジトリ直下の [AGENTS.md](../AGENTS.md) に従う。

## Cursor note
Cursor 向けの rules / skills は `.cursor/rules/` と `.cursor/skills/` を使う。
`.github/skills/` は Copilot 用のコピー。内容を変えるときは `.cursor/skills/` も同じ内容に揃える。
